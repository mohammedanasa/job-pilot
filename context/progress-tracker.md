# Progress Tracker

Update this file after every completed feature. Any AI agent reading this should immediately know what is done, what is in progress, and what is next.

---

## Current Status

**Phase:** Phase 5 — Dashboard
**Last completed:** 17 Analytics Charts — Real Data
**Next:** None — build plan complete (all 17 features)

---

## Progress

### Phase 1 — Foundation

- [x] 01 Homepage
- [x] 02 Auth
- [x] 03 PostHog Initialization
- [x] 04 Database Schema

### Phase 2 — Profile Page

- [x] 05 Profile Page — Full UI
- [x] 06 Profile Save Logic
- [x] 07 AI Profile Extraction from Resume
- [x] 08 Resume PDF Generation from Profile

### Phase 3 — Find Jobs Page

- [x] 09 Find Jobs Page — Full UI
- [x] 10 Adzuna Job Discovery
- [x] 11 Filter + Sort + Pagination

### Phase 4 — Job Details Page

- [x] 12 Job Details Page — Full UI
- [x] 13 Company Research Agent

### Phase 5 — Dashboard

- [x] 14 Dashboard Page — Full UI
- [x] 15 Stats Bar — Real Data
- [x] 16 Recent Activity — Real Data
- [x] 17 Analytics Charts — Real Data

---

## Decisions Made During Build

- 2026-06-06 — Homepage implemented as static Server Components with mock visual assets from `public/`, matching the delivered landing page design before auth logic is wired.
- 2026-06-06 — `lucide-react` is approved in standards but not installed yet, so Feature 01 avoided adding dependencies and used provided brand/product images.
- 2026-06-07 — Feature 02 uses the current InsForge MCP/docs pattern from `@insforge/sdk` and `@insforge/sdk/ssr`. Next.js 16 request interception is implemented with `proxy.ts` instead of legacy `middleware.ts`.
- 2026-06-07 — After OAuth login, users redirect to `/dashboard` so they can complete setup before using the dashboard.
- 2026-06-22 — OAuth callback now exchanges the InsForge PKCE code through a Next Route Handler and writes SDK auth cookies before redirecting to `/dashboard`.
- 2026-06-22 — Auth recovery removed premature PostHog setup/events from Feature 02 and normalized the OAuth route response/error contract.
- 2026-06-22 — Auth review cleanup added explicit return types to the auth helpers, auth components, proxy, and protected placeholder pages.
- 2026-06-22 — Feature 03 initializes PostHog through Next.js `instrumentation-client.ts`, centralizes browser identity helpers in `lib/posthog-client.ts`, and uses request-scoped server captures in `lib/posthog-server.ts` so each server event flushes before returning.
- 2026-06-22 — PostHog tracking is limited to the approved event whitelist: `job_search_started`, `job_found`, `profile_completed`, and `company_researched`.
- 2026-06-22 — Profile page logout calls the InsForge client `auth.signOut()`, posts to `/api/auth/logout` to clear SSR auth cookies, then clears the OAuth verifier, resets PostHog identity, and routes to `/login`.
- 2026-08-12 — Feature 04 rebuilt on a new InsForge backend after the original database was lost. New project `8xcfj34w` (us-east) replaces `4brsdh32` (ap-southeast). Schema recreated from `migrations/20260812175451_create-schema.sql`; the old `20260624131107` migration was deleted.
- 2026-08-12 — `profiles` rows are now created by an `on_auth_user_created` trigger on `auth.users` instead of only by `saveProfile`. All three other tables' `user_id` columns reference `profiles(id)`, so a user without a profile row could not own jobs — signup now guarantees the row exists. `profiles.email` is populated by this trigger and is never written by app code.
- 2026-08-12 — RLS policies scoped `TO authenticated` using `(SELECT auth.uid())` subquery form, paired with explicit `GRANT`s. `anon` has all privileges revoked on all four tables — every route is behind auth.
- 2026-06-24 — Feature 05 Profile Page full UI built with mock data. SVG donut ring completion banner, drag-and-drop resume upload section, full ProfileForm with Personal Info, Professional Info (skills autocomplete from hardcoded list), Work Experience (1 default card, add up to 3), Education, and Job Preferences sections. No save logic — that is Feature 06.
- 2026-08-13 — Feature 06 saves through the `saveProfile` Server Action in `actions/profile.ts`, which upserts the whole form as one row and calls `revalidatePath('/profile')`. `profile_completed` fires only on the transition into completeness — the action reads the existing `is_complete` first so re-saving an already-complete profile does not re-fire the event.
- 2026-08-13 — Resume upload is a Route Handler (`app/api/resume/upload/route.ts`), not the Server Action, because Server Actions are a poor fit for multipart file bodies. It persists `resume_pdf_url` and `resume_pdf_key` immediately on upload, independent of Save Profile, so a resume is never lost by navigating away without saving.
- 2026-08-13 — The `resumes` bucket is private, so a stored URL is not fetchable. `resume_pdf_key` (migration `20260813104026`) holds the storage object key, and all reads go through `app/api/resume/download/route.ts`, which re-authenticates server-side and streams the PDF with `Cache-Control: private, no-store`.
- 2026-08-13 — Completion percentage is computed twice against the same nine required fields: server-side in `app/profile/page.tsx` for the banner, and inside `saveProfile` for the `is_complete` flag. The two lists must stay in sync — if a required field is added, update both.
- 2026-08-13 — **Gemini Flash replaces GPT-4o as the project's model.** Features 08, 10, and 13 were specified against GPT-4o and now use this client. Feature 13's Stagehand browser-automation model is a separate decision, deferred to that feature.
- 2026-08-13 — **The AI layer is provider-neutral: `lib/gemini.ts` became `lib/ai/`** (`index.ts` orchestrator + `types.ts` contract + `gemini.ts` / `groq.ts` adapters). Routes import `@/lib/ai` only. Driven by a real outage: a single provider meant a spent free-tier quota took the whole feature down. `AI_PROVIDER` picks the primary (default `groq`, whose ~1000/day dwarfs Gemini's ~20/minute); quota, availability, and network failures fall through to the other provider. `bad_response` deliberately does *not* fail over — malformed JSON is a prompt/schema problem, so retrying elsewhere spends a second quota for the same answer.
- 2026-08-13 — Groq uses **strict** `json_schema` mode (guaranteed adherence, better than Gemini's best-effort), which is supported only on `openai/gpt-oss-20b` / `120b`. Strict mode demands `additionalProperties: false` and a `required` list naming every property; that adaptation lives in `lib/ai/groq.ts` so shared schemas stay provider-neutral.
- 2026-08-13 — Feature 07 extraction reads the PDF back from storage by `resume_pdf_key` rather than from the request — the file left the browser at upload time. pdf-parse produces text, and the model receives *text*, not the PDF. Sending a scanned PDF straight to the model yields invented fields; a text-length floor of 200 chars catches those before any AI call.
- 2026-08-13 — Extraction fills only resume-derived fields. Job preferences (salary, remote, tone, titles sought) are intentions, not history, and are never overwritten. Work experience is replaced wholesale rather than merged — merging duplicates roles. A dirty form triggers a confirm before overwrite.
- 2026-08-13 — `pdf-parse` and `pdfjs-dist` are listed in `serverExternalPackages` in `next.config.ts`. They must stay there: bundling breaks pdfjs's runtime worker resolution, and the failure appears only when the route is called, never at build time. Feature 08 also handles PDFs and depends on this.
- 2026-08-13 — Extraction distinguishes *our* failures from *the user's*: a worker/environment error returns 500 with "problem on our side", while a genuinely unreadable PDF returns 422 with "try a different file". Reporting infrastructure faults as bad files sends users re-uploading good resumes.
- 2026-08-13 — **Feature 08 writes to a second storage slot, not the uploaded resume's.** The build plan specified `resumes/{user_id}/resume.pdf` with `upsert: true` — the path the uploaded resume already occupies. That would destroy the user's own file and, worse, repoint `resume_pdf_key`, so Feature 07's extraction would afterwards read our AI-written prose back as if it were their resume. Generated PDFs live at `{user_id}/generated-resume.pdf` behind `generated_pdf_url` / `generated_pdf_key` (migration `20260813152602`). `resume_pdf_key` keeps meaning "the file the user gave us".
- 2026-08-13 — The InsForge SDK's `storage.upload(path, file)` takes **two arguments and has no `upsert` option**, contrary to the build plan and `library-docs.md`. Writing an existing key overwrites it silently — verified against the live bucket by uploading the same key three times and reading it back. That is what makes regenerating a resume after a profile edit work.
- 2026-08-13 — Generation reads the **database**, never the form, so `ProfileForm` fingerprints the resume-relevant fields and disables Generate while they differ from the last save. Preferences are excluded from the fingerprint — they never reach the resume, so editing one must not block generation. Without this the PDF silently omits unsaved edits.
- 2026-08-13 — Feature 08 gates on a **minimal subset (full name + one role with responsibilities), not `is_complete`**. `is_complete` also demands salary expectation and cover letter tone, neither of which appears on a resume. Below the minimal floor the model has nothing to ground prose in and starts inventing, so it returns 422 rather than calling the AI.
- 2026-08-13 — **The model writes only prose.** Names, dates, employers, skills, and education are copied verbatim into the PDF; only the summary and role bullets come from `@/lib/ai`. A model that rephrases a job title or nudges a date produces a resume contradicting the user's own record. Roles are matched back by array `index`, not company name — two stints at one employer are common.
- 2026-08-13 — One-page fit is enforced by **bounding the model's output** (summary ≤400 chars, ≤3 bullets per role, ≤3 roles), not by truncating the rendered page. Cutting a bullet mid-sentence looks like a rendering bug; a genuinely dense profile flows to a second page instead of losing content.
- 2026-08-13 — `lib/resume-pdf.tsx` exports `buildResumeDocument()`, not the component. This keeps every piece of JSX out of the route (`app/` holds no UI logic) and types correctly — `createElement()` infers from props and loses the `DocumentProps` shape `renderToBuffer` requires, while a JSX literal inside the route's try/catch trips `react-hooks/error-boundaries`.
- 2026-08-13 — The PDF palette is **literal hex in `lib/resume-pdf.tsx`**, the one sanctioned exception to the no-hardcoded-colour rule. @react-pdf/renderer resolves styles in a PDF layout engine with no CSS custom properties, so `var(--color-text-primary)` resolves to nothing. Values are copied from `ui-tokens.md` and must be updated alongside it.
- 2026-08-13 — Gemini tuning is empirical, not guessed: `thinking_level: "minimal"` plus `maxItems` on arrays plus a `required` list. At `"low"` with no bounds, 1 run in 5 truncated and results populated ~half the fields. Thinking tokens are drawn from `max_output_tokens` and vary run to run, so budgets are generous (4000) and `status: "incomplete"` is checked explicitly.
- 2026-08-14 — Feature 09 built pixel-exact to `context/designs/find-jobs.png` per explicit instruction, which surfaced three conflicts between the delivered design and prior docs/build-plan text, each resolved with the developer:
  - **SOURCE column**: the build plan calls for a Search/URL badge column; the delivered screenshot doesn't show one. Kept it — `jobs.source` is a real, constrained DB column (`'search' | 'url'`), and dropping it now would just mean adding it back later.
  - **Match score thresholds**: `ui-rules.md` and `ui-tokens.md` disagreed with each other, and neither matched the image. Rebuilt to the image's actual pixels — green ≥90%, blue 80-89%, orange below 80% — and corrected both docs so there's one definition going forward.
  - **Navbar**: the design's authenticated navbar (icon-labeled nav items, active item in accent + underline, no marketing CTA) didn't match the shared `Navbar.tsx`, which is homepage-only styling reused as-is on every authenticated route. Built a new `AppNavbar` (`"use client"`, `usePathname`-driven active state) and switched Dashboard, Profile, and Find Jobs to it; the public `Navbar` is untouched. This also corrected `ui-rules.md`'s "no underline" claim.
- 2026-08-14 — Feature 09 is UI-only by design, matching the Feature 05 precedent: every component is a plain Server Component (no `useState`, no handlers) except `AppNavbar`, whose only client requirement is `usePathname()` for nav active-state. Mock data lives in `lib/mock-jobs.ts` as a throwaway `MockJob[]` shape (not the eventual DB `Job` type) so Feature 10/11 aren't constrained by it.
- 2026-08-14 — Feature 10 (Adzuna Job Discovery) built via `/architect`, resolving several conflicts between the build plan and downstream needs:
  - **All scored jobs are saved, not just strong (>=70) matches.** The build plan's banner copy implied only strong matches persist, but Feature 11's Low Match filter (`match_score < 70`) needs weak matches to actually exist in the table. The Find Jobs banner now reads "Found N jobs — M strong matches" instead of "saved M strong matches."
  - **Scoring is one batched AI call for the whole search, not one call per job.** `agent/matcher.ts` sends all ~10 jobs in a single `generateJson` prompt and maps results back by array `index` — the same pattern Feature 08 used for resume role bullets. Verified live against Groq (`openai/gpt-oss-20b`) with a 2-job test batch: correct per-index scores, matched/missing skills. One request against a 30 RPM free tier instead of ten.
  - **The Find Jobs table is wired to real DB data in this feature**, not deferred to Feature 11 as originally scoped — `app/find-jobs/page.tsx` now queries `jobs` for the current user, newest-first, no filter/sort/pagination logic yet. Without this a successful search would be invisible in the UI, which fails `code-standards.md`'s "every feature must be testable" rule.
  - **Duplicate prevention by `source_url`**, checked before scoring (saves AI quota on repeats). No new migration — re-running the same search simply skips URLs already in the user's `jobs` rows.
  - **Profile gate:** a search requires `skills` plus a current title or at least one work-experience entry, checked before any AI call — mirrors Feature 08's minimal-subset gate rather than `is_complete`, which also demands salary expectation and cover letter tone that have nothing to do with matching. Returns 422 with a clear message if unmet.
  - **`lib/utils.ts` created for the first time** — `code-standards.md` mandated `MATCH_THRESHOLD = 70` here since Feature 01, but the file never existed until this feature. Also added `formatRelativeDate()` for the DATE FOUND column, matching Feature 09's mock date strings ("2 hours ago", "Yesterday").
  - **`responsibilities`, `requirements`, `nice_to_have`, `benefits`, `about_company` are left empty/null on inserted rows.** Adzuna's search response is a ~200-char truncated snippet with no structured breakdown for these — inventing them via AI would render as fact on the Job Details page. **Feature 12 must render empty states for these five fields**, not assume they're populated.
  - Live-verified outside the browser (no OAuth session available, same limitation as Feature 09): Adzuna search confirmed live with real API keys against `https://api.adzuna.com/v1/api/jobs/us/search/1` — response shape matches `AdzunaJob` exactly. Groq scoring confirmed live with the exact schema from `agent/matcher.ts`. `.insert(rows).select().single()` and `.update().eq()` confirmed valid — the InsForge SDK's `database` surface delegates to `@supabase/postgrest-js` (`node_modules/@insforge/sdk/package.json`), where both are standard chainable methods.

- 2026-08-14 — Feature 11 (Filter + Sort + Pagination) built via `/architect`:
  - **All four controls live in the URL, not client state.** `?match=&sort=&q=&page=` drives the Server Component, which re-queries InsForge with real `gte`/`lt`/`or`/`order`/`range`. Views are shareable and the back button works, and pagination is genuine `LIMIT`/`OFFSET` rather than slicing an array the page already downloaded in full. `lib/job-filters.ts` owns every URL↔query translation so the page, filter bar, and pagination cannot drift apart.
  - **Default sort is now `match_score` descending, resolving a real UI/query conflict.** Feature 09's `JobsFilterBar` displayed "Match Score" as the selected sort while `page.tsx` actually ordered by `found_at` — the UI stated one thing and the query did another. Score-first also matches the product premise.
  - **`useSearchParams()` was deliberately avoided.** Next 16 docs: a Client Component calling it needs a `Suspense` boundary or *the production build fails, while dev works fine* — the same shape as Feature 07's pdfjs/Turbopack bug. Since `page.tsx` already has the resolved params, it passes them down as plain props; `JobsFilterBar` uses only `useRouter()`/`usePathname()`, neither of which suspends. No `Suspense` boundary anywhere.
  - **`.order("match_score", { ascending: false })` sorts NULLs to the top.** Postgres defaults `DESC` to NULLS FIRST and postgrest-js emits no directive unless given one, so the default view would have led with unscored jobs. Fixed with `nullsFirst: false` — the option is `nullsFirst`; there is no `nullsLast`.
  - **Unscored jobs (`match_score IS NULL`) appear under All Matches only.** An unscored job is unknown, not weak. `gte`/`lt` exclude NULLs by SQL semantics so this needed no extra code, but it does mean such a job is invisible while a match filter is active — accepted knowingly.
  - **`.or()` interpolates user text straight into a PostgREST filter expression and the SDK does not escape it.** `sanitizeSearchTerm` strips LIKE wildcards and escapes `\`/`"`; the value is then double-quoted at the call site so PostgREST's reserved `, . ( ) :` stay literal. Quoting rather than stripping is what keeps a search for `Node.js` working — stripping the dot would make it `nodejs` and match nothing.
  - **Two empty states, not one.** Telling a user with 40 saved jobs to "run a search above" because they mistyped a company name points them somewhere that cannot help. Filtered-empty gets its own copy plus a Clear filters link; `isFiltered()` deliberately ignores sort, since reordering can never empty a result set.
  - **`SearchControls` now resets to the default view after a successful search** instead of only calling `router.refresh()`. Filtered to High Match on page 3, a user would have seen "Found 10 jobs" over a table that did not change.
  - **`Pagination` stays a Server Component** rendering `<Link>`s — no client JS, gets prefetching, supports middle-click. Next's own guidance is `<Link>` over `router.push` absent a specific reason.
  - `build-plan.md`'s 20-per-page kept as specified.

- 2026-08-14 — Feature 12 (Job Details Page — Full UI) built via `/architect`:
  - **The Find Jobs table row is now clickable** — `project-overview.md` specified "Click job row → opens job details page" but Feature 09 never wired it. Implemented as a stretched link (`after:absolute after:inset-0` on the company-cell `<Link>`, `relative` on the `<tr>`) rather than wrapping the row in an anchor, which is invalid HTML. `JobsTable` stays a Server Component.
  - **Header match badge is High/Low at `MATCH_THRESHOLD` (70), not the table's 90/80/below tiers.** The delivered design shows an 85% score as a green pill, which only the threshold rule reproduces — `MatchScoreBar`'s three-tier gradient would render it blue. Two different components now intentionally use two different color rules for match score, both already defined in `ui-tokens.md`'s Status Badges vs Match Score Colors rows.
  - **Only one Job Description card ships — no Responsibilities/Requirements/Nice to Have/Benefits/About Company sections**, despite `project-overview.md` listing all five. Those five `jobs` columns are null on every row sourced from Adzuna search (see Feature 10's note) and the delivered `job-details.png` design shows none of them. Building five permanently-empty sections would be untestable dead UI; they stay in the schema for a possible future structured-extraction feature.
  - **Gap skills render in accent purple (`bg-accent-muted`/`text-accent`), not red/orange.** The build plan said "red/orange badges," but both `ui-tokens.md`'s existing Skills Badges table and the delivered design use accent purple for missing skills. Corrected the build-plan line rather than following it.
  - **Research Company button is inert (`disabled`) in this feature.** `app/api/agent/research/route.ts` already exists as a Feature 13 stub that fires the real `company_researched` PostHog event with a `TODO` in place of actual research. Wiring the button now would write false events into the exact data Feature 17's Company Research Activity chart reads later. `CompanyResearch.tsx` always renders the empty state — it does not yet branch on `job.company_research`.
  - New components under `components/job-details/` follow `architecture.md`'s planned folder exactly: `JobInfo.tsx` (header card + 4-card info row), `MatchScore.tsx` (AI reasoning + skills comparison, returns `null` if both are absent), `JobDescription.tsx`, `CompanyResearch.tsx`, `JobActions.tsx` (Apply Now, returns `null` if no apply URL exists).
  - `app/find-jobs/[id]/page.tsx` queries `jobs` by `id` **and** `user_id` in the same `.eq()` chain (not id alone, then checking ownership after) — a request for another user's job id returns no row and hits `notFound()`, rather than ever materializing another user's data.

- 2026-08-14 — **`/review` fix on Feature 12**: developer reported a job description cut off mid-sentence ending in `…`. Traced end to end — `agent/adzuna.ts` writes `job.description` verbatim (no truncation on our side) and `JobDescription.tsx` rendered it in full with no `.slice()` or `line-clamp`. Called the live Adzuna search API directly with the same query from the report and confirmed **Adzuna's own API caps `description` at exactly 500 characters**, trailing it with `…` server-side — 5/5 sampled results were exactly 500 chars, one matching the reported posting verbatim. This corrects the `~200-char` figure recorded in Feature 10's decision note above — the real cap is 500. There is no more text to recover client-side; `external_apply_url` (Adzuna's redirect to the real posting) is the only place it exists. Fix: `JobDescription.tsx` now takes `applyUrl` and renders a "Read the full description on the original posting →" link whenever `aboutRole.length >= 500`, linking to `external_apply_url`. No change to Feature 10's write path — `about_role` still stores Adzuna's response as-is.

- 2026-08-15 — Feature 13 (Company Research Agent) built via `/architect`, resolving a major gap between the build plan and what's actually installed, plus several smaller conflicts:
  - **No Browserbase, no Stagehand, no GPT-4o.** The build plan specified all three, but none are installed — no `@browserbasehq/*` packages, no `BROWSERBASE_*` env vars, no `OPENAI_API_KEY` (dropped project-wide on 2026-08-13). Adding them meant a new paid service and a second LLM provider outside `lib/ai` for one feature. Rebuilt on plain server-side `fetch()` for the company website plus the existing `lib/ai` layer for synthesis — see the corrected `context/library-docs.md` Company Research section, which replaces the old Stagehand-based one entirely.
  - **One synthesis call, not five.** The build plan's homepage-extract → sub-page-extract → GPT-4o-synthesis shape existed because Stagehand needed a schema per page visit. With plain fetched text there's nothing to distill first, so `agent/research.ts` sends all fetched page text plus the job posting plus the profile into a single `generateJson` call — one request against the 30 RPM tier, matching Feature 10's batching precedent.
  - **ATS-domain guard on homepage derivation.** The build plan's "follow the redirect, strip the subdomain" logic breaks on the common case: Adzuna's `external_apply_url` usually resolves to an ATS host (`boards.greenhouse.io`, `jobs.lever.co`, `*.myworkdayjobs.com`), and stripping the subdomain there yields the ATS vendor's own domain, not the employer's. `lib/company-site.ts` checks the resolved host against a 12-entry ATS blocklist first; a blocked or unresolvable host falls back to guessing `https://www.{slug(company)}.com`, validated with a `HEAD` request before trusting it. Verified live: a real Greenhouse-hosted Anthropic posting correctly fell back to `anthropic.com` instead of resolving to `job-boards.greenhouse.io`.
  - **No `agent_runs` row for a research run.** That table is search-shaped (`job_title_searched`, `jobs_found`) and Feature 16 renders every row as "Found X jobs for [title]" — a research run would show as "Found 0 jobs for —". Feature 16 already reads researched companies from `jobs.company_research` directly, so research logs nothing to `agent_runs`/`agent_logs`; failures go to `console.error`.
  - **Dossier gained a `grounded: boolean` field beyond the build plan's 9 columns.** A profile-and-posting-only dossier reads exactly as confident as a fully-grounded one with nothing to tell them apart — `CompanyResearch.tsx` renders a muted notice ("Based on the job posting only — we couldn't reach {company}'s site") whenever `grounded` is false, driven by this field rather than an empty `sources` array plus hoped-for model hedging.
  - **Research Company button persists as "Re-run Research"** once a dossier exists, rather than disappearing after first use — lets a user whose first attempt hit an ATS dead end or a JS-shell homepage retry. `company_researched` fires per successful run; Feature 15 counts distinct jobs with non-null research, so repeat runs don't inflate that stat.
  - Live-verified against real data, applying the Feature 10 lesson directly (**a toy example proves the schema shape, not that it survives real input sizes**) and catching a real bug from doing so: a 4-page real Stripe research prompt (homepage + 3 sub-pages) at the build's original 2000-char-per-page cap pushed the request to 8152 tokens and Groq rejected it outright (`HTTP 413`, limit 8000 TPM). Cut `MAX_PAGE_CHARS` to 900 and `maxOutputTokens` to 4000; re-verified the same real Stripe prompt at the new caps — `HTTP 200`, `finish_reason: "stop"`, a dossier correctly grounded in Stripe's actual fetched content (tech stack and culture language both traceable to the real pages).
  - Also caught in verification, not initially scoped: with zero fetched pages, the first prompt draft produced a **fully confident, entirely fabricated** company description for a nonexistent test company, despite a general "never invent facts" rule. The "never invent" instruction alone wasn't enough — `buildPrompt()` now branches on `pages.length === 0` with an explicit, separate instruction to hedge ("Based on the job posting, this role appears to involve...") and never state company facts it has no source for. Re-verified: the same nonexistent-company case now returns hedged, posting-grounded language instead of invented specifics.

- 2026-08-15 — Feature 14 (Dashboard Page — Full UI) built exactly to `context/designs/dashboard.png`, surfacing two conflicts between `build-plan.md`'s Feature 14 spec and the delivered design, both resolved with the developer up front rather than discovered mid-build:
  - **4 stat cards, not the build plan's mismatched list.** The build plan named a 4th "Cover Letters Generated" card and a "Resume Tailoring Activity" bar chart; the delivered image shows neither, and `project-overview.md`'s own Dashboard section already lists the correct 4 cards (Total Jobs Found, Avg. Match Rate, Companies Researched, Jobs This Week) and 3 charts (Company Research Activity, Jobs Found Over Time, Match Score Distribution). Cover Letter Generation is explicitly out of scope project-wide, so a "Cover Letters Generated" stat would have had nothing behind it. Built to the image; `build-plan.md`'s Feature 14 UI list should be treated as superseded by this note and by `project-overview.md`.
  - **`recharts` installed and added to `code-standards.md`'s approved dependency list.** It was already the named library for Feature 17's real chart data in both `architecture.md` and `build-plan.md`, but wasn't installed and wasn't on the approved list. Building Feature 14's 3 charts by hand would mean rebuilding them again in Feature 17 just to swap mock data for real data — installed now so Feature 17 is a data change, not a rewrite.
  - **Dashboard incomplete-profile banner is a new compact component (`IncompleteProfileBanner.tsx`), not a reuse of `CompletionBanner.tsx`.** The profile page's existing banner is a full donut-ring component driven by a percentage and a missing-fields list; the dashboard only has `profiles.is_complete` (a boolean) to check, and the delivered dashboard image shows no banner at all (implying the screenshot's account has a complete profile). Built a smaller single-line variant with just a message and a Complete Profile link — consistent card styling, no new visual tokens.
  - **Query reuses the stored `profiles.is_complete` flag directly**, not a fresh recomputation of the 9-field completion check. That check already exists in two places (`app/profile/page.tsx`'s percentage banner and `actions/profile.ts`'s save action) — see the long-standing open question below. Reusing the stored flag for a simple complete/incomplete check avoids adding a third copy of the same field list for a page that doesn't need the percentage, just the boolean.
  - Recent Activity dot coloring: the delivered image alternates dot colors per entry in a way that doesn't cleanly split by activity type (job-search rows are sometimes purple, sometimes green; research rows are consistently blue). Matched the image's exact per-row colors rather than inventing a strict type→color rule: job-search entries alternate `accent`/`success-alt` in order, company-research entries are always `info` — reusing `ui-tokens.md`'s existing three-color Activity Dots table as-is, no new tokens added.
  - All mock data lives in a new `lib/mock-dashboard.ts`, matching the disposable-mock-file precedent `lib/mock-jobs.ts` set in Feature 09 (deleted once Feature 10 wired real data). Expect this file to be deleted or trimmed once Features 15–17 wire the stats, activity, and charts to real data.

- 2026-08-15 — Feature 15 (Stats Bar — Real Data) wires the 4 dashboard stat cards to a single real `jobs` query, resolving one gap the build plan didn't address:
  - **No server-side `AVG`.** The build plan's "Avg. Match Rate — AVG of match_score" logic reads like SQL, but the installed `@supabase/postgrest-js` builder (verified in Feature 11) has no typed aggregate helper — only a raw-string `select=col.avg()` embed that no prior feature has used or verified against this InsForge instance. At this app's per-user data scale, one query pulling `match_score, company_research, found_at` (not `select("*")` — only the 3 columns Feature 15 actually needs) and computing all 4 stats in JS is simpler and equally correct. `lib/dashboard-stats.ts` exports a pure `computeDashboardStats()` taking a narrow `Pick<JobRow, "match_score" | "company_research" | "found_at">[]` — no DB access inside it, matching `architecture.md`'s "components/lib own no data fetching" boundary; the query itself stays in `app/dashboard/page.tsx`.
  - **Trend badges are dropped for all 4 real cards**, not carried over from Feature 14's mock `+12%`/`+3%` deltas. There is no historical-snapshot table anywhere in the schema to compute a real "vs last week" comparison from, and inventing one would be indistinguishable from Feature 13's hallucination bug in spirit — a confident number with no source. `DashboardStat.trend` stays in the type (still consumed correctly by `StatsBar.tsx`, which already conditionally hides an empty trend) so a future feature could add it back if a snapshot mechanism is ever built.
  - **Avg. Match Rate excludes unscored jobs** (`match_score IS NULL`) from both the numerator and denominator, the same semantics Feature 11 already established for the Find Jobs match filters — an unscored job is unknown, not a 0. Renders `"—"` / `"No scored jobs yet"` when zero jobs are scored, rather than `NaN%` or `0%`, both of which would misrepresent an empty state as a real measured rate.
  - **`MockStat`/`MOCK_STATS` deleted from `lib/mock-dashboard.ts`**, replaced by a shared `DashboardStat` type in `types/index.ts` next to `JobRow` (the type it's derived from) — `StatsBar.tsx` now imports the real type, not a mock-only one, since the component itself never depended on mock data, only its former caller did.
  - Verified two ways given the standing no-OAuth-automation gap: (1) `computeDashboardStats()` is a pure function, run directly under `tsx` against representative real-shaped rows (empty array, and a 5-row mix of scored/unscored/researched/unresearched/various-ages) — confirmed correct counts, correct average (81% from 4 scored jobs, 5th excluded), correct 7-day cutoff (3 of 5 rows included, 2 correctly excluded at 8 and 10 days old), and the `"—"` empty-state path; (2) the exact query shape (`select=match_score,company_research,found_at` + `user_id=eq.…`) was probed directly against the live InsForge REST endpoint and returned `401` (auth boundary), not a `PGRST` parse error, confirming PostgREST accepts the column list and filter — same discriminating-test technique Feature 11 and Feature 12 used. `StatsBar` was also re-screenshotted with the same real-shaped data through a temporary route (deleted after) to confirm the no-trend layout still matches the card system visually.

- 2026-08-15 — Feature 16 (Recent Activity — Real Data) wires the dashboard activity feed to `agent_runs` and `jobs`, surfacing and resolving a real schema gap before writing any query, confirmed with the developer up front:
  - **New migration `20260815095002_add-company-researched-at.sql` adds `jobs.company_researched_at` (timestamptz, null until researched).** The build plan says "sort all by created_at descending," but neither table has a `created_at` — `agent_runs` has `started_at`/`completed_at`, and `jobs` had only `found_at`, which is set once at job-*discovery* time (`agent/adzuna.ts`) and never touched by Feature 13's research route. Research is a separate, later action, so `found_at` would show a company researched today as having happened whenever the job was originally found — sometimes weeks off. Applied live via `npx @insforge/cli db migrations up --all` and confirmed present in `information_schema.columns` before writing any app code against it. `app/api/agent/research/route.ts`'s existing `.update({ company_research: result.dossier })` now also sets `company_researched_at: new Date().toISOString()` in the same call — one write, both fields land together, so a job can never have a dossier without a matching timestamp.
  - **Two independent queries, not one.** `agent_runs` (job-search events) and `jobs` filtered on `company_researched_at IS NOT NULL` (research events) are unrelated row sets with different filter columns — there's no single query that produces both. Each is fetched pre-sorted descending and capped at 10 (`ACTIVITY_QUERY_LIMIT`) before merging, which is sufficient because the final merged list only ever needs the top 10 overall and each source is already in the right order.
  - **`agent_runs` is filtered to `status = "completed"`, not all runs.** A `running` row has no `completed_at` to sort or display by, and a `failed` run isn't something to show off in an activity feed — "Found 0 jobs for Backend Engineer" from a crashed search would look like a real (bad) result rather than an error. `lib/recent-activity.ts`'s `buildRecentActivity()` also defensively filters out any row with a null `completed_at`/`company_researched_at` it's handed, independent of the caller's query filter, so the merge logic can't be broken by a future caller that queries more broadly.
  - **Merge-and-format logic lives in a new pure function**, `buildRecentActivity()` in `lib/recent-activity.ts` — no DB access inside it, same `lib/` boundary `lib/dashboard-stats.ts` established in Feature 15. It takes narrow row types (`AgentRunRow`, `ResearchedJobRow`) matching exactly what `app/dashboard/page.tsx` selects, merges both arrays, sorts by actual timestamp (not grouped by type), formats each into the exact copy the build plan specifies ("Found X jobs for [jobTitle]" / "Researched [company]") via the existing `formatRelativeDate()` from Feature 09/11, and slices to 10.
  - **`RecentActivity.tsx`'s dot-coloring logic is untouched** — it already consumed a generic `{ id, type, text, timestamp }` shape from Feature 14/15's `MockActivity`, so swapping the import to the new `ActivityEntry` type (identical shape, real source) required no component logic changes, only a type-source swap.
  - `MockActivity`/`MOCK_ACTIVITY` deleted from `lib/mock-dashboard.ts`, matching Feature 15's precedent for `MockStat`/`MOCK_STATS`.
  - Verified: the new column's existence was confirmed live via `information_schema.columns` before use; both new query shapes (`agent_runs` filtered/ordered by `completed_at`, `jobs` filtered on `company_researched_at IS NOT NULL` ordered descending) were probed directly against the live InsForge REST endpoint and returned `401` (auth boundary), not a parse error; `buildRecentActivity()` was run directly under `tsx` against representative data proving correct cross-source sort order (not grouped by type), correct defensive null-filtering of an incomplete run even without the caller's status filter, and correct 10-item capping from 15 input rows; `RecentActivity` was re-screenshotted with the same real-shaped merged data via a temporary route (deleted after), confirming both the populated and empty states render correctly; the research route's existing 400/401 boundaries were re-confirmed live, unchanged by adding the second field to its update call.

- 2026-08-15 — Feature 17 (Analytics Charts — Real Data) built via `/architect`, and the data source changed entirely from what the build plan specified — investigated and resolved with the developer before writing any code:
  - **PostHog is not queryable from this codebase, at all, by any available means.** Four independent checks, all negative: (1) `posthog-node` 5.38.2's full public API surface was enumerated from its `.d.ts` — `capture`, `identify`, `alias`, feature flags, groups, exceptions — there is no query/insights/HogQL method anywhere in the package; (2) the PostHog MCP server requires OAuth the developer hasn't granted, so the `posthog:querying-posthog-data` skill can't run either; (3) `.env.local` holds only the project *write* token and the EU ingestion host (`eu.i.posthog.com`) — no personal API key (`phx_…`, `query:read` scope) and no numeric project ID, both of which the Query API requires and neither of which exists yet; (4) even with full API access, the Match Score Distribution chart would still have no data behind it — `app/api/agent/find/route.ts`'s `job_found` capture only ever sent `{ userId, source }`, never `matchScore`, despite `library-docs.md`'s PostHog section documenting a `matchScore` property that was never actually implemented. `library-docs.md`'s PostHog section is corrected to remove that stale example.
  - **All three charts wired to the InsForge `jobs` table instead**, which already holds everything needed. Feature 15's existing dashboard query (`match_score, company_research, found_at`) gained one more column, `company_researched_at` (added in Feature 16), and that's the only query change — no new query, no new round trip. Three new pure functions in `lib/analytics-charts.ts` (`buildJobsFoundOverTime`, `buildCompanyResearchActivity`, `buildMatchScoreDistribution`) do the bucketing, same `lib/`-owns-no-data-fetching boundary as `dashboard-stats.ts` and `recent-activity.ts`.
  - **Day buckets are UTC, not the viewer's local timezone.** A Server Component has no browser timezone to read, and `lib/utils.ts`'s `formatRelativeDate()` already set the precedent of being timezone-naive in this codebase — this is consistent with that, not a new gap. Both day-bucketed charts (`Jobs Found Over Time`, 30 days; `Company Research Activity`, 7 days) always emit the full window with explicit zero-fill, so a day with no activity renders as a real zero bar/point rather than being silently absent from the x-axis.
  - **Match Score Distribution's bucket boundaries are exclusive-except-the-top-range**, so a score of exactly 60 lands in "60-70%" only, never double-counted into "50-60%" too, and 100 is captured by "90-100%"'s inclusive upper bound rather than falling off the end.
  - **Charts distinguish "no data at all" from "a populated window with real zero days."** `hasData` is passed as a separate prop from `data`, computed by the caller from the raw `jobs` array (`dashboardJobs.length > 0`, or `.some(job => field !== null)`), not derived from whether every bucket in `data` happens to be zero — an account with 5 real jobs all older than 30 days would otherwise show an all-zero chart that looks broken instead of an honest "no jobs found yet" empty state.
  - **New chart empty state matches `RecentActivity.tsx`'s existing text style exactly** (`text-sm font-medium text-text-muted`, centered in the same `h-64` slot the chart fills) — first time this dashboard has needed more than one component with an empty state, so this is now the reusable pattern rather than a one-off.
  - `lib/mock-dashboard.ts` deleted — Feature 17 was its last remaining consumer after Features 15 and 16 already removed the stats and activity mocks.
  - **Build plan fully complete as of this feature** — all 17 planned features across all 5 phases are now code-complete.

---

## Notes

- 2026-06-06 — `npm run build` passes for the completed homepage.
- 2026-06-07 — `npm run build` passes for InsForge OAuth auth, callback handling, refresh route, protected routes, and placeholder protected pages.
- 2026-06-22 — `npm run build` passes after moving OAuth completion to server-owned cookie exchange.
- 2026-06-22 — `npm run lint` and `npm run build` pass after auth review fixes.
- 2026-06-22 — `npm run lint` and `npm run build` pass after explicit return type cleanup.
- 2026-06-22 — `npm run lint` and `npm run build` pass for Feature 03 PostHog Initialization.
- 2026-06-22 — `npm run lint` and `npm run build` pass after adding profile page logout with SSR auth cookie clearing.
- 2026-08-12 — `npm run lint` and `npm run build` pass after the Feature 04 schema rebuild. Signup trigger verified end to end: inserting an `auth.users` row auto-creates the matching `profiles` row, and deleting the user cascades it away.
- 2026-08-13 — `npm run lint` and `npm run build` pass for Feature 06. Backend verified live: migration `20260813104026_add-resume-pdf-key` is applied (both `resume_pdf_url` and `resume_pdf_key` present on `profiles`), and the `resumes` bucket exists with `public: false`.
- 2026-08-13 — ✅ **Feature 07 confirmed working by the developer in the browser** on a real resume — the full click-to-populate path (Extract button → route → pdf-parse → Groq → form fields), which no automated check covered.
- 2026-08-13 — Feature 07's Gemini adapter (then `lib/gemini.ts`, now `lib/ai/gemini.ts`) verified against the live Gemini API with a generated test resume: extraction returns every field correctly — name, phone, location, LinkedIn, title, `experienceLevel` "Senior", 8 years, 9 skills, both roles with dates, `currentlyWorking` inferred from "Present", and degree mapped to the form's exact `Master's` enum value.
- 2026-08-13 — ⚠️ That first verification ran under `tsx` (plain Node), **not** inside Next, and so did not exercise the route. It missed a bug that made the feature completely non-functional: pdfjs's worker does not resolve under Turbopack, so every extraction threw and was reported to the user as a bad PDF. Fixed with `serverExternalPackages` and then re-verified *inside the running Next dev server* (640 chars parsed from a real PDF). **Lesson: `npm run build` passing proves a module imports, not that it runs. Verify server code by calling it in the actual runtime.**
- 2026-08-13 — Groq verified end to end **inside the Next dev server** (not just under `tsx`): pdf-parse → `lib/ai` → Groq returned a complete, correct extraction with `AI_PROVIDER` unset, confirming the groq-by-default path. Fallback verified in both directions — with `AI_PROVIDER=gemini` and Gemini either rate-limited or holding an invalid key, the request still succeeded via Groq. Strict-schema adaptation handled nested objects, arrays, and enums correctly on the first attempt.
- 2026-08-13 — `npm run lint` and `npm run build` pass for Feature 08. Migration `20260813152602_add-generated-resume-columns` applied live and verified (`generated_pdf_url` and `generated_pdf_key` both present on `profiles`).
- 2026-08-13 — ✅ Feature 08 verified **inside the running Next dev server**, applying Feature 07's lesson rather than trusting the build. A temporary unauthenticated probe route called `renderToBuffer` under Turbopack and returned a valid `%PDF-` buffer, then was deleted. **`@react-pdf/renderer` needs no `serverExternalPackages` entry** — unlike pdf-parse, it resolves cleanly under Turbopack. The real route was then confirmed to load and reject unauthenticated requests with 401.
- 2026-08-13 — The rendered PDF was inspected visually, not just byte-checked: header, accent title, section rules, right-aligned date ranges (`2021-03` → "Mar 2021 — Present"), bullets, skills, and education all lay out correctly on one page. Note `backgroundColor` and `flex` **do** work, despite `library-docs.md` listing a narrower set of supported properties.
- 2026-08-13 — The prose schema was verified against live Groq: nested objects, the integer `index` field, and bounded arrays all pass strict `json_schema` mode. A first run leaked **first person** into the summary ("I lead platform teams") despite a third-person rule in the prompt; the rule was rewritten as an explicit no-pronoun instruction with an example, and three consecutive runs then came back clean. Resume prose is generated at temperature 0.7, so single-run checks prove little — repeat them.
- 2026-08-13 — ⚠️ The Gemini free tier rate-limits aggressively: the cap is ~20 requests per **minute** (`generate_content_free_tier_requests`), and it trips constantly during development. Correction to an earlier note here: this was first recorded as a *daily* exhaustion, which was wrong — the 429 body reads `limit: 20 ... Please retry in Ns`. The adapter now parses that retry hint into the user-facing message instead of guessing. In practice the window can outlast the hint, which is exactly why the Groq fallback was added. Usage is visible at https://ai.dev/rate-limit.
- 2026-08-14 — `npm run lint` and `npm run build` pass for Feature 10. Verified inside the running dev server per the Feature 07 lesson: `/find-jobs` still 307-redirects unauthenticated requests; `POST /api/agent/find` correctly rejects a missing body (400) and an unauthenticated request (401) live. Adzuna's live API was called directly with the real `.env.local` keys and returned real IT-jobs results matching `AdzunaJob`'s shape exactly (`title`, `company.display_name`, `location.display_name`, `redirect_url`, `salary_min`, `description`). Groq was called directly with `agent/matcher.ts`'s exact schema and a 2-job test batch — both jobs scored correctly by index, with sensible matched/missing skills, confirming the batched-scoring approach works end to end.
- 2026-08-14 — Not verified: a full search through the real UI with a logged-in session (no OAuth automation available, same gap Feature 09 has). The InsForge insert/update calls in `agent/adzuna.ts` were sanity-checked by inspecting `@insforge/sdk`'s dependency on `@supabase/postgrest-js` rather than exercised live — `createInsforgeServer()` is the same cookie-scoped, RLS-respecting client `actions/profile.ts` already writes through successfully, so this is considered low-risk but not proven.
- 2026-08-14 — `npm run lint` and `npm run build` pass for Feature 11. `/find-jobs` builds as `ƒ` (dynamic), confirming `searchParams` opts it out of prerendering as intended.
- 2026-08-14 — Feature 11's two genuinely-unproven query behaviours were probed live against the project before being called done, per the Feature 07/10 lesson. **The `.or()` quoting is confirmed by a discriminating test, not an assumption:** PostgREST parses the filter tree *before* checking permissions, so an unauthenticated probe still distinguishes syntax from auth. The unquoted form `company.ilike.%Smith, Inc%,…` returned `400 PGRST100 "failed to parse logic tree"`, while the quoted form returned `401 42501 permission denied` for all five test terms — `acme`, `Node.js`, `Smith, Inc`, `a(b)c`, `with space` — proving each parsed successfully. **`count: "exact"` confirmed** against the live REST endpoint: `content-range: 0-19/20`, i.e. the post-slash total is present and ignores `limit`, and `match_score.desc.nullslast,found_at.desc` ordered correctly. Probe script deleted after the run.
- 2026-08-14 — ⚠️ **Not yet verified in the browser, and pagination cannot be verified with current data.** The account holds exactly 20 jobs — precisely `PAGE_SIZE` — so the page-number strip is hidden (one page) and no second page exists. At least one more search returning a non-duplicate job is needed before pagination has ever rendered. Current distribution is 7 high / 13 low / 0 unscored, so High and Low are testable now but the NULL-score path has no data to exercise it.
- 2026-08-14 — **Bug found in the developer's own browser testing, fixed via `/recover` (Failure Mode 1)**: `agent/matcher.ts`'s scoring prompt had no input or output bound. My 2-job toy-example verification during the build didn't surface it — real Adzuna descriptions plus a full `work_experience` JSON dump across up to 10 jobs was large enough to trip Groq's request limit outright (`HTTP 400`) on one run, and on another exhausted the fixed 4000-token reply budget mid-batch (`response truncated`), surfacing to the user as "The AI response was cut short." Fixed by: capping each job description at 600 chars before prompting (`MAX_DESCRIPTION_LENGTH`), narrowing `work_experience` in the prompt to a short "title at company" list instead of the full JSON blob, tightening `matchReason` to 1-2 sentences (≤200 chars, was "one paragraph" ≤400) and `matchedSkills`/`missingSkills` to 8 items (was 15), and raising `maxOutputTokens` from 4000 to 6000. Re-verified live against Groq with 10 real, untruncated Adzuna job descriptions (real-world lengths, not the toy example) — `finish_reason: "stop"`, all 10 scores returned correctly indexed. **Lesson: a toy-example verification (2 short jobs) proved the schema shape works, but not that it survives at real data volume — the two are different claims, and only the second one is the one that matters in production.**
- 2026-08-14 — `npm run lint` and `npm run build` pass for Feature 12. `/find-jobs/[id]` builds as `ƒ` (dynamic). Verified inside the running dev server per the Feature 07 lesson: an unauthenticated request to `/find-jobs/00000000-0000-0000-0000-000000000000` 307-redirects to `/login?next=%2Ffind-jobs%2F...`, confirming the route is registered and protected identically to every other authenticated page. The exact `.eq("id", …).eq("user_id", …).single()` query shape was probed directly against the live InsForge REST endpoint (`/api/database/records/jobs` with `Accept: application/vnd.pgrst.object+json`) — PostgREST parsed the filter and returned `401` (auth boundary), not a parse error, confirming the query syntax is valid. Full click-through with a real job and a logged-in session was not possible — no OAuth automation available, the same gap every prior Find Jobs feature has noted.
- 2026-08-15 — `npm run lint` and `npm run build` pass for Feature 13. `/api/agent/research` builds as `ƒ` (dynamic). Verified inside the running dev server: `POST` with an empty body returns `400` ("jobId is required"), and a well-formed body with no session returns `401` before any DB or AI call runs. `/find-jobs/[id]` still 307-redirects unauthenticated requests, confirming the page-level auth boundary is unchanged.
- 2026-08-15 — Feature 13's full pipeline was verified live outside the browser (no OAuth automation available, the same gap every prior feature has noted), and this is the deepest live verification any feature has had so far because the plan changed mid-build: `lib/company-site.ts`'s homepage derivation was run against a real Adzuna-shaped Stripe posting (correctly resolved to `stripe.com`) and a real Greenhouse-hosted Anthropic posting (correctly blocked and fell back to guessing `anthropic.com`, confirmed resolvable via a live `HEAD` request). The fetch → strip → cap → synthesize pipeline was then run end to end against Stripe's real homepage and 3 real sub-pages (679KB raw HTML fetched and reduced to capped text) through the real Groq endpoint with the real dossier schema — first at the build's original 2000-char-per-page cap, where it failed with a real `HTTP 413` at 8152/8000 tokens, then again at the corrected 900-char cap, where it returned `HTTP 200`, `finish_reason: "stop"`, and a dossier whose tech-stack and culture claims trace back to Stripe's actual page content. The profile-only fallback path (zero fetched pages) was also run twice against the real API — once surfacing the hallucination bug described in the Decisions log above, once after the prompt fix confirming hedged, posting-grounded output. Not verified: the actual `POST /api/agent/research` route end to end with a real job row and a logged-in browser session, and the InsForge `.update({ company_research })` write itself was not exercised live — it follows the exact `.eq("id", …).eq("user_id", …)` shape already proven live in Feature 12's read path, so this is considered low-risk but not proven, same caveat as Feature 10's insert path.
- 2026-08-15 — `npm run lint`, `npx tsc --noEmit`, and `npm run build` all pass for Feature 14. `/dashboard` still builds as `ƒ` (dynamic). The auth boundary was confirmed live in the running dev server exactly as every prior protected route: an unauthenticated `curl` to `/dashboard` returns `307` to `/login?next=%2Fdashboard`, unchanged from before this feature. Since no OAuth automation exists to reach the page as a logged-in user (the standing gap noted every feature since 09), visual verification used a temporary unauthenticated route rendering the same components with the same mock data — screenshotted with Playwright/Chromium, compared side by side against `context/designs/dashboard.png`, confirmed matching layout, spacing, colors, and chart shapes, then deleted before finishing. A second temporary route confirmed the incomplete-profile banner (not shown in the design, which implies a complete-profile account) renders correctly above the stat cards. Both temporary routes were removed and the full lint/tsc/build suite re-run clean afterward to confirm no leftover trace.
- 2026-08-15 — `npm run lint`, `npx tsc --noEmit`, and `npm run build` all pass for Feature 15. `/dashboard` still builds as `ƒ`; the unauthenticated `307` redirect is unchanged. `computeDashboardStats()` was run directly under `tsx` (not just typechecked) against an empty array and a 5-row realistic mix — see the Feature 15 decision note above for the exact assertions. The `select=match_score,company_research,found_at&user_id=eq....` query was probed directly against the live InsForge REST endpoint and returned `401`, confirming PostgREST parses the column list and filter correctly rather than rejecting it as malformed. A temporary route re-rendered `StatsBar` with the same real-shaped computed stats and was screenshotted, confirming the card layout holds with trend badges absent on all 4 cards; the route was deleted immediately after.
- 2026-08-15 — `npm run lint`, `npx tsc --noEmit`, and `npm run build` all pass for Feature 16. `/dashboard` still builds as `ƒ`; the unauthenticated `307` redirect and the research route's `400`/`401` boundaries are all unchanged. Migration `20260815095002_add-company-researched-at` was applied live with `npx @insforge/cli db migrations up --all` and the new column's presence was confirmed via a direct `information_schema.columns` query before any app code depended on it. Both new query shapes were probed directly against the live InsForge REST endpoint (`agent_runs` filtered/ordered by `completed_at`; `jobs` filtered on `company_researched_at=not.is.null` ordered descending) and both returned `401`, confirming PostgREST parses the filters correctly. `buildRecentActivity()` was run directly under `tsx` against representative multi-source data, confirming true chronological interleaving (not grouped by type), defensive exclusion of a null-`completed_at` row, and the 10-item cap from a 15-row input. `RecentActivity` was re-screenshotted through a temporary route with the same real-shaped merged data, confirming both the populated and empty states render correctly; the route was deleted immediately after.
- 2026-08-15 — `npm run lint`, `npx tsc --noEmit`, and `npm run build` all pass for Feature 17. `/dashboard` still builds as `ƒ`; the unauthenticated `307` redirect is unchanged. The three new pure functions in `lib/analytics-charts.ts` were run directly under `tsx` against an empty array and a realistic multi-row mix: confirmed the 30-day and 7-day windows always emit their full length with correct zero-fill, entries outside each window are excluded (a 31-day-old job correctly absent from the 30-day chart), score-bucket boundaries don't double-count (a score of exactly 60 lands only in "60-70%") and the top bucket's upper bound is inclusive (100 counted in "90-100%"), and null/out-of-range scores are excluded from every bucket. **First genuine live browser verification this entire dashboard phase has had**: a pre-existing dev server (from an earlier, separate session) was found already running with a real authenticated user actively navigating `/find-jobs/…` → `/dashboard` while this feature was being built. Its logs showed transient `ReferenceError: MOCK_COMPANY_RESEARCH_ACTIVITY is not defined` errors at the moment `lib/mock-dashboard.ts` was deleted mid-edit — expected Turbopack HMR lag, not a real bug — and the log was clean on every recompile afterward, meaning that real user's real `/dashboard` load rendered successfully against this feature's finished code with their actual DB data. A temporary `app/dev-preview-charts` route (populated + empty variants of all three charts, real-shaped data) was also used to confirm chart titles and empty-state copy render correctly via HTML inspection over `curl`; no Playwright/screenshot tool was available in this session, so pixel-level visual confirmation (as done for Features 09–14) was not repeated here — the live authenticated render is considered stronger evidence than a synthetic screenshot would have been anyway. The temporary route was deleted and the full lint/tsc/build suite re-run clean afterward.
